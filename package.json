{
  "name": "bunnyblogmern",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "prebuild-client": "node scripts/createConfig.js",
    "build-client": "cd client && npm run build",
    "build": "npm run install-server && npm run install-client && npm run prebuild-client && npm run build-client",
    "start": "node server/server.js",
    "install-server": "cd server && npm install",
    "install-client": "cd client && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@auth0/auth0-react": "^2.2.4",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-jwt": "^8.4.1",
    "jwks-rsa": "^3.1.0",
    "mongoose": "^7.4.3",
    "nodemon": "^3.0.1",
    "react-quill": "^2.0.0"
  },
  "description": "",
  "proxy": "http://localhost:8000"
}
